import max from './max';
import min from './min';
import mandatory from './mandatory';
import same from './same';

const Validation = {
  max,
  min,
  mandatory,
  same,
};

export default Validation;